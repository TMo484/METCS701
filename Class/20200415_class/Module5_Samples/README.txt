
Running the Module5 Samples

HTTP Samples

1) angular9-samples-http

  % cd angular9-samples-http
  % npm install
  % ng serve --open
    (Browser opens with http://localhost:4200)

  Before going to the next set of samples, kill the process with CTRL-C

Routing Samples (AngularSamplesRouting folder)

2) angular9-routing-sample1

  cd angular9-routing-sample1
  npm install
  ng serve --open
    (Browser opens with http://localhost:4200)

Before going to the next set of samples, kill the process with CTRL-C

3) angular9-routing-sample2

  cd angular9-routing-sample2
  npm install
  ng serve --open
    (Browser opens with http://localhost:4200)

Before going to the next set of samples, kill the process with CTRL-C

4) angular9-routing-sample3

  cd angular9-routing-sample3
  npm install
  ng serve --open
    (Browser opens with http://localhost:4200)

Before going to the next set of samples, kill the process with CTRL-C


